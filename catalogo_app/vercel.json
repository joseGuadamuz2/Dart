{
  "framework": null,
  "installCommand": "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git .vercel_flutter && .vercel_flutter/bin/flutter config --no-analytics",
  "buildCommand": ".vercel_flutter/bin/flutter pub get && printf 'API_URL=%s\\n' \"$API_URL\" > .env && if [ -n \"$PUBLIC_CATALOG_URL\" ]; then printf 'PUBLIC_CATALOG_URL=%s\\n' \"$PUBLIC_CATALOG_URL\" >> .env; fi && .vercel_flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}